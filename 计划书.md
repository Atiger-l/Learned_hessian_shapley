# 实习课题开展计划书

**程嘉麟**
**2026年4月16日**
**课题题目：面向大模型参数重要性分析的可学习曲率代理方法探索**

---

## 一、课题理解与选题背景

### 1.1 问题背景

大语言模型（LLM）的参数重要性分析是模型压缩、知识注入和可解释性研究的核心课题。根据文献调研， Model Shapley（NeurIPS 2025）提出了一种基于 Shapley 值的参数重要性评估框架，本课题整体的工作将以此为启发和基础来进行。
对于模型内部参数的重要性，以往的代表方法如saliency map，通过计算一阶导数定义了loss对哪些参数更敏感。这种方法的局限性是只单独考虑每一个参数的"重要性"，忽略了参数之间的协同效应。Model Shapley的核心insight在于将模型的各参数间的协同效应也纳入参数重要性的衡量中。将每个参数视作一个"局中人"，运用博弈论中的Shapley值量化参数的重要性，将Shapley值中的效用函数定义为将对应参数集合置0后的负loss，并用路径积分经过合理近似推出了二阶 Taylor 展开的数学形式，将参数级 Shapley Score的计算简化为单次反向传播：
$$
\phi_\theta(\mathbf{U}) := \frac{1}{M} \sum_{k=1}^M \binom{M-1}{k-1}^{-1} \sum_{\substack{\Theta_S \subseteq \Theta \setminus \{\theta\} \\ |\Theta_S| = k-1}} [\mathbf{U}(\Theta_S \cup \{\theta\}) - \mathbf{U}(\Theta_S)]
$$

$$
\mathbf{U}(\Theta) := -\mathcal{L}(\Theta; x, y)
$$

经过数学推导和合理近似，参数i的Shapley Score可写为：

$$
\phi_i \approx -g_i^\top\theta_i - \frac{1}{2}\theta_i^\top(H\theta)_i
$$

其中 $g_i = \partial\mathcal{L}/\partial\theta_i$ 为梯度，$H$ 为损失函数的 Hessian 矩阵。由于完整 Hessian 的存储复杂度为 $O(M^2)$（7B 模型约需 196TB），所以关键在于估计 Hessian ，原方法采用经验 Fisher 信息矩阵（FIM）作为 Hessian 的可计算代理：

$$
H \approx F(\Theta) = \mathbb{E}_{(x,y)\sim\mathcal{D}}\left[(\nabla_\Theta\mathcal{L})(\nabla_\Theta\mathcal{L})^\top\right]
$$


### 1.2 现有方法的局限性

通过精读原论文（重点聚焦于Appendix A.4、A.5，对于FIM估计的阐述），发现FIM近似的一些局限性：

Fisher 矩阵仅依赖梯度外积，无法感知当前任务的 loss landscape 局部几何特征，对不同任务使用相同的近似形式。在计算不同下游应用的shapley score缺乏一定的针对性。并且固定的近似公式无法随 checkpoint 变化自适应调整，在模型不同训练阶段的曲率估计质量参差不齐。原论文 Appendix A.5 明确指出，在非平稳训练条件（如 continual learning、early-stage training）下，FIM 的保真度会显著下降，无法有效估计Hessian矩阵。

**理论分析：Fisher 矩阵等价于 Hessian 的条件**

Fisher 矩阵与 Hessian 的等价性依赖于严格的理论前提。对于参数化模型 $p_\Theta(y|x)$，损失函数取负对数似然 $\mathcal{L} = -\log p_\Theta(y|x)$ 时，Hessian 可分解为：

$$
H = \underbrace{\mathbb{E}\left[(\nabla_\Theta \log p_\Theta)(\nabla_\Theta \log p_\Theta)^\top\right]}_{= F(\Theta),\ \text{Fisher 矩阵}}
  + \underbrace{\mathbb{E}\left[-\nabla^2_\Theta \log p_\Theta\right]}_{\text{残差项}\ R(\Theta)}
$$

仅当残差项 $R(\Theta) \approx 0$ 时，$H \approx F(\Theta)$ 成立。该条件等价于模型输出分布 $p_\Theta(y|x)$ 与数据真实分布完全匹配（模型已收敛至全局最优）。在实际的 LLM 应用场景中，此条件几乎不成立：

- **模型未完全收敛**：预训练或微调过程中，$p_\Theta$ 与真实分布存在显著偏差，残差项 $R(\Theta)$ 不可忽略，Fisher 矩阵系统性地低估了 Hessian 的曲率。
- **分布偏移**：下游任务数据分布与预训练分布不同，导致 Fisher 矩阵在目标任务上的近似质量进一步下降。
- **非凸 loss landscape**：LLM 的 loss landscape 高度非凸，局部曲率结构复杂，梯度外积这一秩一近似无法捕捉 Hessian 的完整谱结构。

与其他固定近似方法（K-FAC 假设 Kronecker 可分结构、SparseGPT 使用激活外积）相比，上述方法均依赖对 Hessian 结构的先验假设，无法从数据中自适应地学习任务相关的曲率模式，这正是本课题引入可学习代理的核心动机。

### 1.3 改进思路

本课题的核心改进是：将 Hessian 近似从**固定的 Fisher 矩阵**改进为**可学习的神经网络** $f_\phi$，将Hessian矩阵的代理做成一个带结构约束的神经网络，希望在保留可拓展性的同时，提高曲率代理对具体任务和具体checkpoint的适配能力。
在直观上，本工作可以理解为：当一个选定参数block(层，注意力头，神经元...)处于当前的状态(包含权重、梯度、激活值和层位置信息等)时，在特定任务特定checkpoint上，其loss landscape会有多弯、沿各个方向的灵敏度等曲率信息。

探索两种建模形式：

**方案 A：可学习对角近似**

$$\hat{h}_b = f_\phi(z_b) \in \mathbb{R}^{d_b}$$

输入仅为参数块状态特征 $z_b = \{\Theta^{(b)}, \nabla_{\Theta^{(b)}}\mathcal{L}, a^{(b)}, \ell\}$，输出对角曲率估计。学习"该参数块处于某种状态时，各参数维度的曲率大小，即自曲率$H_{ii}$"。

**方案 B：可学习 HVP 算子（主要方案）**

$$\widehat{Hv} = f_\phi(z_b,\, v) \in \mathbb{R}^{d_b}$$

同时输入状态特征 $z_b$ 和探测方向 $v$，直接学习 Hessian-vector product 算子。学习"该参数块处于某种状态时，沿方向 $v$ 的曲率响应"——这是学习 Hessian 的映射，无需学习显示 Hessian 矩阵。

在 Shapley Score计算中，令 $v = \theta_b$ 即可直接得到所需的 $H\theta$ 近似：

$$\phi_i \approx -g_i^\top\theta_i - \frac{1}{2}\theta_i^\top \underbrace{f_\phi(z_b,\, \theta_b)}_{\widehat{H\theta_b}}$$

**相较于方案 A 的优势**：方案 B 相比 Fisher 矩阵和方案 A，能够感知探测方向 $v$ 的信息，从而捕捉 loss landscape 在不同方向上的各向异性曲率结构，这是固定公式所无法表达的。并且通过学习完整 HVP 可以刻画参数间的协同效应，这一点是方案 A 只学习对角信息无法实现的。

---

## 二、初步调研结果

文献与代码的调研

| 论文 | 核心贡献 | 与本课题的关联 |
|------|---------|--------------|
| [Model Shapley (NeurIPS 2025)](https://github.com/Artessay/ModelShapley) | Shapley 值参数重要性，Fisher 近似 Hessian | 直接基础工作 |
| [SparseGPT (Frantar et al., 2023)](https://arxiv.org/abs/2301.00774) | 基于 Hessian 的 one-shot LLM 剪枝 | 证明 Hessian 信息在 LLM 中的有效性 |
| [GPTQ (Frantar et al., 2022)](https://arxiv.org/abs/2210.17323) | 基于 Hessian 的逐层量化 | 与 Model Shapley 集成的量化 baseline |
| [WoodFisher (Singh & Alistarh, 2020)](https://arxiv.org/abs/2004.14340) | 高效逆 Hessian-vector product | HVP 计算的工程参考 |
| [K-FAC (Martens & Grosse, 2015)](https://arxiv.org/abs/1503.05671) | Kronecker 积近似 Fisher | 结构化曲率近似的代表方法 |
| Hutchinson (1990) | 随机 Hessian 对角估计 | 监督信号构造的核心工具（用于方案A内的对角阵的估计） |
| [Better Hessians Matter (Bae et al., 2025)](https://arxiv.org/abs/2509.23437) | 更好的 Hessian 近似 → 更好的 influence score | 直接支持本课题动机 |
| [HVP Series (Rathore et al., 2023)](https://arxiv.org/abs/2310.14901) | 用 HVP 级数近似 Hessian 逆 | HVP 作为基本计算单元的理论基础 |

**各文献详细介绍**

**Model Shapley (NeurIPS 2025)**：本课题的直接基础工作。该工作将合作博弈论中的 Shapley 值引入大模型参数重要性分析，通过路径积分推导出二阶 Taylor 展开的闭式近似，将参数级 Shapley Score 的计算简化为单次反向传播。为规避完整 Hessian 的 $O(M^2)$ 存储代价，采用经验 Fisher 信息矩阵（FIM）作为 Hessian 代理，并在知识注入、模型压缩、可解释性三个下游任务上验证了有效性（如 Qwen2.5-14B INT8 量化 MMLU 达 76.27%，显著优于 GPTQ 的 65.20%）。论文 Appendix A.5 明确承认 FIM 在非平稳训练条件下保真度下降，这是本课题的直接改进切入点。

**SparseGPT (Frantar et al., 2023)**：提出基于 Hessian 的 one-shot LLM 剪枝方法，在不重新训练的情况下将 LLM 剪枝至 50% 稀疏度而几乎不损失精度。其核心是利用权重矩阵输入激活的外积近似逐层 Hessian，并通过 Cholesky 分解高效求解最优权重补偿。该工作有力证明了 Hessian 信息在 LLM 压缩中的有效性，同时也说明 Hessian 近似的质量直接决定了下游任务的效果上限。

**GPTQ (Frantar et al., 2022)**：基于 Hessian 的逐层量化方法，将每层权重的量化误差建模为带 Hessian 加权的最优化问题，通过贪心逐列量化和误差补偿实现高精度低比特量化。Model Shapley 将 GPTQ 作为量化 baseline 并与 Shapley-guided 量化对比，本课题改进 Hessian 近似后可直接在此对比框架上验证效果。

**WoodFisher (Singh & Alistarh, 2020)**：提出高效计算逆 Hessian-vector product 的方法，通过 Woodbury 矩阵恒等式将逆 Hessian 的计算复杂度从 $O(d^3)$ 降至 $O(d \cdot n)$，其中 $n$ 为用于估计 Fisher 的样本数。该工作为本课题方案 B 中 HVP 的高效计算提供了工程参考，其递推更新公式也可用于在线构造 teacher signal。

**K-FAC (Martens & Grosse, 2015)**：提出用 Kronecker 积分解近似 Fisher 矩阵的结构化方法，将每层的 Fisher 矩阵近似为输入激活协方差与梯度协方差的 Kronecker 积，在保留块结构的同时大幅降低存储和计算代价。K-FAC 是目前最具代表性的结构化曲率近似方法，但其 Kronecker 可分假设在实际网络中并不严格成立。本课题的可学习代理不依赖任何结构假设，可视为对 K-FAC 类固定近似的数据驱动替代。

**Hutchinson's Estimator (Hutchinson, 1990)**：提出用随机向量估计矩阵迹的无偏方法：$\text{tr}(A) \approx \frac{1}{K}\sum_k v_k^\top A v_k$，其中 $v_k \sim \text{Rademacher}(\pm 1)$。AdaHessian（Yao et al., 2021）将其推广到估计 Hessian 对角并应用于深度学习优化。本课题方案 A 直接采用该方法构造 teacher signal：令 $A = H_b$，通过 PyTorch 二阶自动微分精确计算 $Hv_k$，取均值得到每个神经元的 $\text{diag}(H)$ 估计。

**Better Hessians Matter (Bae et al., 2025)**：系统研究了 Hessian 近似质量对 influence function 计算精度的影响，发现更准确的 Hessian 近似能显著提升 influence score 的可靠性，进而改善数据归因、模型调试等下游任务的效果。该工作从实验角度直接支持了本课题的核心动机：Hessian 近似的质量是参数重要性评估的关键瓶颈，值得专门优化。

**HVP Series (Rathore et al., 2023)**：提出用 Hessian-vector product 的幂级数展开近似 Hessian 逆，为以 HVP 为基本计算单元构建高阶曲率估计提供了理论基础。本课题方案 B 将 HVP 作为学习目标，与该工作的思路一脉相承——HVP 是连接可计算性与曲率信息的核心桥梁。

**调研结论**：目前尚无工作将 Hessian 近似做成可学习的、任务自适应的神经网络并应用于参数重要性评估。现有方法（Fisher、K-FAC、SparseGPT 的激活外积近似）均为固定公式，没有从数据的角度学习曲率模式，存在明确的研究空白。

下载阅读 ModelShapley 官方代码仓库（`Artessay/ModelShapley`），定位到其核心方法calculate_cooperative_interactions的代码：

**`neural_function.py` 中的 Fisher 近似**（第 ~160 行）：
```python
def calculate_cooperative_interactions(param):
    # Approximate Hessian matrix by Fisher information matrix
    hessian_matrix = torch.matmul(param.grad, param.grad.T)
    cooperative_interactions = torch.sum(
        param * torch.matmul(hessian_matrix, param), dim=1
    )
    return cooperative_interactions
```

这是本课题的**直接改进目标**：将 `torch.matmul(param.grad, param.grad.T)` 替换为可学习的 $f_\phi(z_b)$。

监督信号的构造思路：

**方案 A 对角 Hessian**

使用 Hutchinson estimator 无偏估计 $\text{diag}(H_b)$，复杂度 $O(Kn)$，K为Hutchinson方法的取样数，可根据实际情况调节，无需存储完整矩阵：

$$\text{diag}(H) \approx \frac{1}{K}\sum_{k=1}^K v_k \odot Hv_k, \quad v_k \sim \text{Rademacher}(\pm 1)$$

其中 $Hv$ 通过 PyTorch 二阶自动微分精确计算（`torch.autograd.grad` with `create_graph=True`）。该方法由 Hutchinson(1990)提出用于估计矩阵迹，AdaHessian（Yao et al., 2021）将其推广到估计 Hessian 对角并应用于深度学习。
具体而言：$f=\nabla L \cdot v$是一个标量函数，$f: \mathbb{R}^{d_b} \to \mathbb{R}$，$f$对各个参数用自动微分求梯度就得到了HVP即$Hv$。
**方案 B HVP**

对每个训练样本随机采样探测方向 $v$（随机/梯度/参数方向），通过二阶自动微分精确计算 $Hv$，构成 $(z_b, v, Hv)$ 训练三元组：

```python
grads = torch.autograd.grad(loss, params, create_graph=True)
grad_v = sum((g * v).sum() for g, v in zip(grads, vs))
hvp = torch.autograd.grad(grad_v, params)  # 精确 Hv
```

**网络结构**：
- 方案 A ： $f_\phi$ 为共享 MLP，输入为每行的完整参数+梯度拼接（维度 $2d_b+2$），输出该行的曲率标量，跨层共享权重，参数量与 $d_b$ 无关（延迟初始化）。
- 方案 B ： 相较于方案 A ，$f_\phi$ 的输入信号除$z_b$外，还有$v$，$z_b$（维度 $2d_b+2$）和 $v$（维度 $d_b$）分别经线性层压缩到隐层维度后 element-wise 交互，再投影回 $d_b$，可跨层共享。

**实验设置**：主实验使用 Qwen2.5-3B-Instruct，与ModelShapley原论文实验设置一致。

---

## 三、技术方案

### 3.1 整体框架

整个流程先从某个 checkpoint 和一小批 probe data 出发，逐层逐行提取每个神经元的状态。这里一个 block 就是权重矩阵的一行（对应一个神经元），每个神经元的特征向量是 $z_{b,i} = [\theta_{\text{row}_i},\ g_{\text{row}_i},\ \text{layer\_idx},\ \text{block\_type}]$，也就是把这行的所有权重、对应的梯度、层索引和块类型拼在一起。

接下来构造监督信号，两种方案分开做。方案 A 用 Hutchinson 方法估计每个神经元对应的 Hessian 对角元作为 teacher signal；方案 B 则随机采样探测方向 $v$，用 PyTorch 自动微分精确算出 $Hv$，得到 $(v, Hv)$ 对作为训练数据。

然后训练代理网络：方案 A 训练一个网络 $f_\phi$，输入 $z_b$，输出 $\hat{h}_b$（即该行各维度的曲率估计）；方案 B 训练的网络输入 $(z_b, v)$，输出 $\widehat{Hv}$（即沿方向 $v$ 的曲率响应）。

得到 $\hat{H}_\phi$ 之后，把它替换掉 Model Shapley 原代码里的 Fisher 近似 $g \cdot g^\top$，按公式 $\phi_i \approx -g_i^\top\theta_i - \frac{1}{2}\theta_i^\top(\hat{H}_\phi\theta)_i$ 计算 Shapley 值。

最后在知识注入、量化、可解释性等下游任务上对比 Fisher baseline 和可学习代理的效果。
### 3.2 Block 粒度与特征设计

**Block 定义**：与 ModelShapley 保持一致，以 weight matrix 的**一行**为一个 block（对应一个神经元/输出通道），Shapley 打分粒度为 per-neuron。

**特征向量** $z_{b,i}$（第 $i$ 行）：

$$z_{b,i} = \left[\underbrace{\theta_{\text{row}_i}}_{d_{\text{col}}},\ \underbrace{g_{\text{row}_i}}_{d_{\text{col}}},\ \underbrace{\text{layer\_idx}}_{1},\ \underbrace{\text{block\_type}}_{1}\right] \in \mathbb{R}^{2d_{\text{col}}+2}$$

直接保留每行的完整参数值和梯度，不做统计量压缩，保留方向信息。$f_\phi$ 跨层共享权重，输入维度通过延迟初始化自动适配。

### 3.3 两种方案的训练目标

**方案 A（对角近似）**：

$$\mathcal{L}_A(\phi) = \mathbb{E}_{b,i}\left[\left|\hat{h}_\phi(z_{b,i}) - \text{diag}(H_b)_i\right|^2\right]$$

输出层使用 Softplus 自然满足 PSD 约束（非负对角）。

**方案 B（HVP 算子）**：

$$\mathcal{L}_B(\phi) = \mathbb{E}_{b,v}\left[\left\|f_\phi(z_b, v) - H_b v\right\|_2^2\right]$$

每次随机采样探测方向 $v$（random/param/gradient），用精确 HVP 作为 teacher。

### 3.4 训练流程

**数据准备**：从目标任务数据集（如 MMLU、C4）中采样 128~512 条作为 calibration set $\mathcal{D}_{\mathcal{F}}$，即 ModelShapley 原论文中的 probe dataset。该数据集不用于训练 LLM 本身，仅用于训练 $f_\phi$ 并使曲率估计反映目标任务特性。

**方案 A 训练循环**：每个 batch 先做一次保留计算图的前向+反向，然后采样 $K$ 个 Rademacher 向量，通过二阶自动微分算出精确的 $Hv_k$，取均值得到每个神经元的 $\text{diag}(H)$ 作为 teacher signal。同时提取当前 batch 的参数行和梯度行拼成特征 $z_b$，送入 CurvatureSurrogate 得到预测值，用 MSE 对 teacher 做监督，更新 $f_\phi$。

**方案 B 训练循环**：同样保留计算图做前向+反向，每次随机采样一个探测方向 $v$（随机/参数/梯度方向之一），用二阶自动微分精确计算 $Hv$，构成 $(z_b, v, Hv)$ 三元组。将 $z_b$ 和 $v$ 一起送入 HVPSurrogate，输出对 $Hv$ 的预测，用 MSE 做监督更新 $f_\phi$。

**关键超参数**：

| 参数 | 推荐值 | 说明 |
|------|--------|------|
| calibration set 大小 | 128 samples | 反映目标任务曲率分布 |
| Hutchinson 采样数 $K$ | 10~20 | 越大越准，但每次需 $K$ 次二阶反向 |
| 训练轮数 | 30~50 epochs | $f_\phi$ 参数量小，收敛快 |
| 学习率 | 1e-3 (Adam) | — |
| batch size | 4 | 受 GPU 显存限制 |

**计算开销说明**：方案 A 每个 epoch 需 $K$ 次二阶反向传播，在 3B 模型上开销较大。实际可通过减少 $K$（$K=5$ 已有不错估计质量）或仅对 MLP 层计算 teacher signal 来降低开销。

已基于 ModelShapley 官方代码框架完成初步实现，代码仓库已初始化（含 Git 记录，2 个 commit）：

- `src/neural_function.py`：核心模块，包含：
  - `compute_hessian_diag_hutchinson()`：方案 A teacher signal，通过 `create_graph=True` 二阶自动微分计算真正的 $Hv$，而非 Fisher 代理
  - `compute_hvp()`：方案 B teacher signal，支持 random/param/gradient 三种探测方向
  - `extract_block_features()`：神经元级特征提取，每行保留完整参数值+梯度，不做统计量压缩
  - `CurvatureSurrogate`：方案 A 模型，延迟初始化 MLP，跨层共享权重
  - `HVPSurrogate`：方案 B 双编码器模型
  - `calculate_shapley_learned_a/b()`：替换 Fisher 的 Shapley 计算接口
- `src/run_experiment.py`：端到端 pipeline
- `src/evaluate.py`：下游任务评估（4 种方法 PPL/Kendall's τ 对比）
- `src/ablation.py`：消融实验脚本

---

## 四、实验计划

### 4.1 核心对比实验（必做）

| 方法 | Hessian 近似 | 评估指标 |
|------|-------------|---------|
| Gradient-only | 无 | MMLU acc, WikiText-2 PPL |
| Fisher（原始） | $g \cdot g^\top$ | 同上 |
| **Learned Diagonal（本方案）** | $f_\phi(z_b)$ | 同上 |

### 4.2 消融实验

**A. 特征消融**：验证激活特征的贡献
- 仅梯度 / 梯度+参数 / 梯度+参数+激活 / 全部特征

**B. Probe 方向消融**：验证 teacher signal 质量
- 随机方向 / 参数方向 / 梯度方向

**C. Calibration 数据量消融**：
- 16 / 32 / 64 / 128 samples

### 4.3 分析实验

- **排序一致性**：Kendall's τ 衡量 Fisher vs Learned 参数重要性排序的相关性，分析两种方法在参数选择上的差异
- **曲率近似质量**：在小规模模型上计算精确 Hessian，对比相对误差 $\|\hat{H} - H_{\text{exact}}\|_F / \|H_{\text{exact}}\|_F$
- **失败案例分析**：识别哪些层（如 embedding 层、最后几层）的曲率最难学习，分析原因并提出改进思路

---

## 五、计划和目标

| 时间 | 任务 | 目标 |
|------|------|--------|
| 第1周（4.16–4.22） | 精读 ModelShapley 代码，在 Qwen2.5-3B 上复现 Fisher baseline | baseline 数值结果 |
| 第2周（4.23–4.29） | 实现 Hutchinson teacher signal + 块级特征提取 pipeline | 数据 pipeline 跑通，teacher signal 质量验证 |
| 第3周（4.30–5.6） | 训练 $f_\phi$，集成到 Shapley 框架，运行核心对比实验 | Fisher vs Learned 主要实验结果 |
| 第4周（5.7–5.13） | 消融实验 + 失败案例分析 + 报告撰写 | 最终报告与代码仓库 |

---

## 预期交付物

1. **代码仓库**（含 Git 记录，README 与运行说明）
2. **可复现实验脚本**（含数据处理与评估流程）
3. **实习报告**（PPT/PDF），包含：
   - 问题分析与范围界定
   - Model Shapley 方法与 Fisher 近似局限分析
   - 可学习曲率代理的技术方案与建模设计
   - Baseline 与改进方案对比
   - 实验设计与结果（含对比实验与失败案例分析）
   - 深入分析：HVP 学习 vs Fisher proxy 的差异

---


## 参考文献

1. **Model Shapley**: Find Your Ideal Parameter Player via One Gradient Backpropagation. NeurIPS 2025. [[Code]](https://github.com/Artessay/ModelShapley)
2. **SparseGPT**: Massive Language Models Can Be Accurately Pruned in One-Shot. Frantar et al., 2023. [[arXiv:2301.00774]](https://arxiv.org/abs/2301.00774)
3. **GPTQ**: Accurate Post-Training Quantization for Generative Pre-trained Transformers. Frantar et al., 2022. [[arXiv:2210.17323]](https://arxiv.org/abs/2210.17323)
4. **WoodFisher**: Efficient Second-Order Approximation for Neural Network Compression. Singh & Alistarh, 2020. [[arXiv:2004.14340]](https://arxiv.org/abs/2004.14340)
5. **K-FAC**: Optimizing Neural Networks with Kronecker-factored Approximate Curvature. Martens & Grosse, 2015. [[arXiv:1503.05671]](https://arxiv.org/abs/1503.05671)
6. **Hutchinson's Estimator**: A stochastic estimator of the trace of the influence matrix for Laplacian smoothing splines. Hutchinson, 1990.
7. **AdaHessian**: An Adaptive Second Order Optimizer for Machine Learning. Yao et al., 2021. [[arXiv:2006.00719]](https://arxiv.org/abs/2006.00719)
8. **Better Hessians Matter**: Studying the Impact of Curvature Approximations in Influence Functions. Bae et al., 2025. [[arXiv:2509.23437]](https://arxiv.org/abs/2509.23437)
9. **HVP Series**: Series of Hessian-Vector Products for Tractable Saddle-Free Newton Optimisation of Neural Networks. Rathore et al., 2023. [[arXiv:2310.14901]](https://arxiv.org/abs/2310.14901)

# Install PyTorch FIRST with a CUDA build that matches `nvidia-smi` top-right "CUDA Version"
# (e.g. CUDA 12.x on the driver: use cu124 wheels). Default pip install torch may pull +cu130
# which needs a newer driver/toolkit pairing and can yield torch.cuda.is_available()==False.
#
#   pip uninstall -y torch torchvision torchaudio
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124
#
# Then:
#   pip install -r requirements.txt
#
torch>=2.0
transformers>=4.36
datasets>=2.14
accelerate>=0.26
tqdm
scipy
